{
  "name": "cmv",
  "version": "2.0.2",
  "description": "Contextual Memory Virtualisation — git-like snapshot and branching for Claude Code sessions",
  "type": "module",
  "bin": {
    "cmv": "./dist/index.js"
  },
  "files": [
    "dist/",
    "LICENSE",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "docs": "typedoc",
    "link": "npm run build && npm link",
    "postinstall": "test -f dist/postinstall.js && node dist/postinstall.js || echo \"CMV: run 'npm run build' then 'npm install' to auto-install hooks.\"",
    "prepublishOnly": "npm run build && npm run test:run"
  },
  "keywords": [
    "claude",
    "claude-code",
    "session",
    "snapshot",
    "branch",
    "context"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/CosmoNaught/claude-code-cmv.git"
  },
  "homepage": "https://github.com/CosmoNaught/claude-code-cmv",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@inkjs/ui": "^2.0.0",
    "chalk": "^5.0.0",
    "commander": "^12.0.0",
    "ink": "^5.0.0",
    "react": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "@types/react": "^18.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typedoc": "^0.27.9",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
